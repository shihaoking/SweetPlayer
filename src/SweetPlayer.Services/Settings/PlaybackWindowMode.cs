namespace SweetPlayer.Services.Settings;

/// <summary>
/// 播放窗口的默认启动模式。
/// </summary>
public enum PlaybackWindowMode
{
    /// <summary>
    /// 窗口模式（默认）。
    /// </summary>
    Windowed = 0,

    /// <summary>
    /// 全屏模式。
    /// </summary>
    FullScreen = 1,
}
